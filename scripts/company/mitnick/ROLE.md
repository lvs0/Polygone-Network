# MITNICK — Security Lead

**Mission** : threat-commodity/high-value, kill-switch, sandbox `ProtectHome`/`wasmi fuel`, duress 4-file.
**Brique** : Faradey + Cube + Security
**KPI** : audit interne, `duress --confirmer` testé

## Responsabilités
- Threat models : commodity vs high-value distincts
- Kill-switch : `duress --confirmer` détruit 4 fichiers
- Sandbox : `ProtectHome=yes`, `InaccessiblePaths=~/.polygone`, wasmi fuel
- Zeroize documenté (pqcrypto n'expose pas de bytes mutables)

## Règle d'or
L'honnêteté radicale : deux threat models séparés, chaque promesse = test ou commande.

